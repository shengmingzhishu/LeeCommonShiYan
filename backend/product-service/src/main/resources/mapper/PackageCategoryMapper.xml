<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lingli.product.mapper.PackageCategoryMapper">

    <resultMap id="BaseResultMap" type="com.lingli.product.entity.PackageCategory">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="name" property="name" jdbcType="VARCHAR"/>
        <result column="code" property="code" jdbcType="VARCHAR"/>
        <result column="parent_id" property="parentId" jdbcType="BIGINT"/>
        <result column="sort_order" property="sortOrder" jdbcType="INTEGER"/>
        <result column="status" property="status" jdbcType="INTEGER"/>
        <result column="created_at" property="createdAt" jdbcType="TIMESTAMP"/>
        <result column="updated_at" property="updatedAt" jdbcType="TIMESTAMP"/>
        <result column="created_by" property="createdBy" jdbcType="VARCHAR"/>
        <result column="updated_by" property="updatedBy" jdbcType="VARCHAR"/>
        <result column="is_deleted" property="isDeleted" jdbcType="INTEGER"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, name, code, parent_id, sort_order, status, created_at, updated_at, created_by, updated_by, is_deleted
    </sql>

    <!-- 获取启用的分类列表，按排序字段排序 -->
    <select id="selectEnabledCategories" parameterType="com.baomidou.mybatisplus.extension.plugins.pagination.Page" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM package_categories
        WHERE status = 1
        AND is_deleted = 0
        ORDER BY sort_order ASC, id ASC
        <if test="size > 0">
            LIMIT #{offset}, #{limit}
        </if>
    </select>

    <!-- 根据父分类ID获取子分类 -->
    <select id="selectByParentId" parameterType="map" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM package_categories
        WHERE parent_id = #{parentId}
        AND status = 1
        AND is_deleted = 0
        ORDER BY sort_order ASC, id ASC
    </select>

</mapper>